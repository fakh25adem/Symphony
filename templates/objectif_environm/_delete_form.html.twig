<form method="post" action="{{ path('app_objectif_environm_delete', {'id': objectif_environm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objectif_environm.id) }}">
    <button class="btn">Delete</button>
</form>
