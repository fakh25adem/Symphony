<form method="post" action="{{ path('app_categorie_action_delete', {'id': categorie_action.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_action.id) }}">
    <button class="btn">Delete</button>
</form>
